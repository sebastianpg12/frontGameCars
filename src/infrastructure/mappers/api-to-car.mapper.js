export const apiToCarMapper = ({ carId, laneId, gameId, driverId, goal, currentPosition, advance }, playerName) => ({
  carId,
  laneId,
  gameId,
  driverId,
  goal,
  currentPosition,
  advance,
  playerName,
});
