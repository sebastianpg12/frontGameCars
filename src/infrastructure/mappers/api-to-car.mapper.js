export const apiToCarMapper = ({ carId, laneId, gameId, driverId, goal, currentPosition, advance, namePlayer }) => ({
  carId,
  laneId,
  gameId,
  driverId,
  goal,
  currentPosition,
  advance,
  namePlayer,
});
